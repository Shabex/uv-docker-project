[project]
name = "java"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shabex", email = "shabsibrah@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
java = "java:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
